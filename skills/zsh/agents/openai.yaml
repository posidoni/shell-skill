# yaml-language-server: $schema=../../../schemas/openai-skill-metadata.schema.json
interface:
  display_name: "Zsh"
  short_description: "Fast safe zsh scripting patterns"
  brand_color: "#2563EB"
  default_prompt: "Use $zsh to review this zsh function or startup file."
policy:
  allow_implicit_invocation: true
