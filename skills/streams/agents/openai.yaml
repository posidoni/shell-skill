# yaml-language-server: $schema=../../../schemas/openai-skill-metadata.schema.json
interface:
  display_name: "Streams"
  short_description: "Correct stdin stdout stderr use"
  brand_color: "#2563EB"
  default_prompt: "Use $streams to fix stdin, stdout, stderr, and redirection behavior."
policy:
  allow_implicit_invocation: true
