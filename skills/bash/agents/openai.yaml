# yaml-language-server: $schema=../../../schemas/openai-skill-metadata.schema.json
interface:
  display_name: "Bash Safety"
  short_description: "Robust Bash scripts and portability"
  brand_color: "#2563EB"
  default_prompt: "Use $bash to write or review a robust portable Bash script."
policy:
  allow_implicit_invocation: true
