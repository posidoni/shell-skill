# yaml-language-server: $schema=../../../schemas/openai-skill-metadata.schema.json
interface:
  display_name: "Shebang"
  short_description: "Portable interpreter lines"
  brand_color: "#2563EB"
  default_prompt: "Use $shebang to choose the right interpreter line for this script."
policy:
  allow_implicit_invocation: true
