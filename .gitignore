# --- OS / editor cruft ---------------------------------------------------
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# --- Tooling caches ------------------------------------------------------
# Lefthook per-developer local overrides (not committed).
lefthook-local.yml
# bats temporary run directories, if ever configured to persist.
.bats-tmp/

# --- Local, per-developer environment ------------------------------------
# Secrets and machine-local settings must never be committed. The tracked
# template is examples/standards, not a real .env.
.env
.env.*
!.env.example
*.local

# --- Credentials / keys (belt and suspenders; nothing here should exist) --
*.pem
*.key
id_rsa
id_ed25519

# --- Agent working files (never part of the product) ---------------------
# Per-developer Claude Code state: local permission overrides and Ralph loop
# scaffolding. The shared, committed config (if any) lives in .claude/settings.json.
.claude/settings.local.json
.claude/ralph-loop.local.md
.ralph/
